# myfunc : 핵심 로직을 구현한 함수
def myfunc():
    # 로그인 되어 이는지 여부를 확인

    print('myfunc')

    # 로직이 끝났다. 안내문


def myfunc2():
    # 로그인 되어 이는지 여부를 확인

    print('myfunc2')

    # 로직이 끝났다. 안내문


def myfunc3():
    # 로그인 되어 이는지 여부를 확인

    print('myfunc3')

    # 로직이 끝났다. 안내문